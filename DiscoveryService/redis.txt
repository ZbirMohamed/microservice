## My dictionary of reads ##
    ? --> means optional 

set (key) (value) == dict["key"] = value
get (key) == dict.get(key)

get (aa) // a no existing key == (nil) | dict.get("aa") == null
//lpush push the new data in the left side so that the last element will always be the first indexed in the array 
Lpush (key) (values == string) (value) ==> ["value2","value1"]
//does the inverse of LUSH
RPUSH fruits ()
get fruits (start) (end) == get fruits 0 -1 (-1 always the last element)
lpop (key) // this will always remove the first element
rpop (key) // the last element

sadd (key) (values) // create a set
smembers // shows the set elements

HSET (key) key value key value key value // hash set element of pairs key and value
hget (key) key? // you can get the element either by its main key tag or get a specific value
// ex : hget user1 name ==> "amine" --> this gives you the user name | print(dict["name"])
// or hget user gives you all the elements keys and values | print(dict)

sessions
set (session_name) (value) ex (time in secondes) // sets a session that will expire with time
ttl (session_name) // shows you the time till expire
get (session_name) // shows you the session value

sorting arrays
SORT  !!! for array od doubles number integers

ZSETS (sorted sets)
ZADD (set name) (score == number) name 
ZRANGE (set name) (start) (end) (withscores)  // gives you the (value , key) sorted 
HGETALL user-service


## set notification for key expiration
set 